services:
  app:
    build:
      context: .
      target: ${DOCKER_TARGET:-dev}
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
